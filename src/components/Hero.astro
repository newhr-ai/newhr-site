<section class="hero">
  <p class="eyebrow">AI FOR HR. BY HR.</p>

  <h1>
    HR AI Use Cases Prepared by HR.
  </h1>

  <p class="lead">
    Practical examples from real HR operations.
    No theory. No vendor language.
  </p>
</section>

<style>
  .hero {
    padding-block: var(--space-xl);
  }

  .eyebrow {
    margin-bottom: var(--space-sm);

    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.18em;

    color: var(--color-teal);
  }

  h1 {
    max-width: 10ch;
    margin-bottom: var(--space-md);

    font-size: clamp(4rem, 10vw, 5.5rem);
    line-height: 0.95;
    letter-spacing: -0.04em;
  }

  .lead {
    max-width: 38rem;

    font-size: 1.25rem;
    line-height: 1.5;
  }
</style>